/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatars.githubusercontent.com',
      'localhost',
      'storage.googleapis.com',
      'https://spacetime-web-seven.vercel.app',
    ],
  },
}

module.exports = nextConfig
